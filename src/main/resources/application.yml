spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wchallenge?serverTimezone=UTC
    username: root
    password: 
    initialization-mode: always
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      dialect: org.hibernate.dialect.MySQL5Dialect
  thymeleaf:
    cache: false
server:
  port : 9090
